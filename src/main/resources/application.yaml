spring:
  application:
    name: api

  datasource:
    url: jdbc:mysql://localhost/volimed_api
    username: root
    password: ${DB_PASSWORD}

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true